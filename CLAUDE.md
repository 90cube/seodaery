# CLAUDE.md

---

## 1 스크립트 1 원칙

하나의 파일은 역할 중 **정확히 하나**만 담당한다.
역할이 두 가지 이상이면 **분리 먼저, 코드 작성 나중.**

| 역할 | 규칙 |
|------|------|
| 진입점 | 등록/초기화/해제만. 로직 금지 |
| 도메인 로직 | 외부 프레임워크 의존 금지. 순수 언어만 |
| UI / 뷰 | 로직 금지 → 도메인 레이어 호출만 |
| 데이터 / 모델 | 상태 정의만. 연산 금지 |
| 시스템 | 단일 책임 기능 단위 |
| 유틸 / 상수 | 부수효과 없는 순수 함수. 하드코딩 금지 |

---

## 코딩 규칙

```
명명: 언어 컨벤션 준수, 프로젝트 내 통일
경로·상수: 하드코딩 금지 → 상수 파일로 분리
저장 포맷: 프레임워크 네이티브 우선, JSON은 외부 호환용만
Undo: 상태 변경은 반드시 Undo 스택 경유
단일 파일 200줄 초과 금지 → 즉시 분리
```

---

## 에이전트 워크플로우

### 기본 흐름

```
Scout (기존 코드 파악) → Architect (설계 검토) → Builder (구현)
```

- **"바로 진행"** → Architect → Builder 직행
- **"플랜 보여줘"** → Architect 출력 확인 후 Builder 투입

### Phase 규칙

- 이전 Phase 미완료 시 다음 Phase 진입 금지
- Phase 완료 시 `.build/{phase}_done.json` 생성
- 핵심 PoC Phase 실패 시 이후 Phase 전면 재설계

### 팀 에이전트 (파일 5개+ 또는 레이어 동시 작업 시)

| 역할 | 담당 |
|------|------|
| Architect | 레이어 경계 설계, 1원칙 감리 |
| Builder | 코드 작성 |
| QA | 레이어 침범·명명 불일치·CANNOT 위반 감사 |

**운영 규칙:**

1. 설계 플랜 + 체크리스트 먼저 → 코드 나중
2. 독립 레이어 간 동시 작업은 반드시 병렬
3. Phase 완료마다 테스트 통과 확인 후 다음 진입

---

## CANNOT 가드레일

1. **도메인 레이어에서 외부 프레임워크 직접 호출 금지** — UI·진입점 레이어에서만
2. **진입점 파일에 로직 작성 금지** — 등록/해제만
3. **단일 파일 200줄 초과 금지** — 초과 시 즉시 분리
4. **UI 레이어에 상태 저장 금지** — 상태는 데이터/모델 레이어에서
5. **버튼명은 행동 직결형** — ~~Apply~~ / ~~Run~~ → 구체적 동사 + 대상
6. **신규 도메인 용어는 용어집 등록 후 코드 작성**
